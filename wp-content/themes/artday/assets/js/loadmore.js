jQuery(function($){	$('#true_loadmore').click(function(){		var preloader = '<div class="woss_preloader"><img src="'+woss_data.woss_template_directory_uri+'/assets/img/ajax-loader.gif"/></div>';			$('#woss_blog_preloader').append(preloader).fadeIn(); // text of the button		var data = {			'action': 'loadmore',			'query': true_posts,			'page' : current_page		};		$.ajax({			url:ajaxurl, // handler			data:data, // data			type:'POST', // query type			success:function(data){				if( data ) { 					$('#true_loadmore').text('Load more posts').before(data); // new posts					current_page++; // increase number of the page for 1					if (current_page == max_pages) $("#true_loadmore").remove(); // if is last page, remove the button				} else {					$('#true_loadmore').remove(); // if is last page, hide the button				}			},			complete:function(){				$('.woss_preloader').remove();			}		});	});});